% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{attack}
\alias{attack}
\title{Attack a statistical claim}
\usage{
attack(
  model,
  term = NULL,
  data = NULL,
  outcome = NULL,
  cluster = NULL,
  profile = c("default", "clinical", "social_science", "prediction", "strict", "fast"),
  attacks = NULL,
  intensity = c("normal", "fast", "deep", "insane"),
  alpha = 0.05,
  alternative = c("two.sided", "less", "greater"),
  kill_rule = c("p_over_alpha", "ci_crosses_zero", "sign_flip", "effect_below_threshold"),
  effect_threshold = NULL,
  seed = 1,
  parallel = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{model}{A fitted \code{lm}, \code{glm}, \code{aov}, \code{lme4::lmer}, \code{lme4::glmer}, or
\code{survival::coxph} model. \code{htest} objects return an explicit limited-support
result.}

\item{term}{Character scalar naming the coefficient or test term to attack.
If \code{NULL}, falsifyr attacks the first non-intercept coefficient.}

\item{data}{Optional data frame used to refit the model. When omitted,
falsifyr attempts to recover the model data.}

\item{outcome}{Optional character vector of user-supplied placebo outcome
names for the placebo attack family.}

\item{cluster}{Optional character scalar naming a grouping variable for a
grouped row-deletion attack. Supply \code{data} when the grouping variable is
not part of the fitted formula.}

\item{profile}{Character scalar choosing an attack profile. Profiles tune the
default attack-family emphasis when \code{attacks = NULL}; \code{profile = "fast"}
also defaults to \code{intensity = "fast"} when intensity is not supplied.}

\item{attacks}{Character vector of attack families. \code{NULL} runs the default
families.}

\item{intensity}{Character scalar controlling attack breadth: \code{"fast"},
\code{"normal"}, \code{"deep"}, or \code{"insane"}.}

\item{alpha}{Significance level used by kill rules.}

\item{alternative}{Character scalar defining the claim direction for
coefficient tests: \code{"two.sided"}, \code{"less"}, or \code{"greater"}.}

\item{kill_rule}{Character scalar defining what kills a claim. Supported
rules are \code{"p_over_alpha"}, \code{"ci_crosses_zero"}, \code{"sign_flip"}, and
\code{"effect_below_threshold"}.}

\item{effect_threshold}{Numeric threshold used by
\code{"effect_below_threshold"}.}

\item{seed}{Integer seed for deterministic attack runs.}

\item{parallel}{Logical; if \code{TRUE}, independent attack families run on at
most two local workers.}

\item{verbose}{Logical; if \code{TRUE}, prints progress messages for expensive
\code{"insane"} attack runs.}
}
\value{
A \code{falsifyr_attack} object with the extracted claim, attack
leaderboard, smallest kill, survival score, verdict, runtime metadata, and
warnings.
}
\description{
Runs a collection of adversarial robustness checks against a fitted model
claim. The returned object summarizes whether the claim survives each attack,
the smallest perturbation that kills it, and an overall survival score.
}
\examples{
fit <- lm(score ~ treatment + age + baseline_score, data = fragile_trial)
result <- attack(fit, term = "treatment", attacks = "row_deletion", intensity = "fast")
result
}
